# Versions injected at build time (see maven-plugin/build.gradle.kts
# processResources). Single source of truth: gradle.properties for the wirespec
# / extractor coordinates. The IR emitter + Kotest DSL extension resolve at
# wirespecVersion (they ship as wirespec artifacts).
wirespecVersion=${wirespecVersion}
extractorVersion=${extractorVersion}
